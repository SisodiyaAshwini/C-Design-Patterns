Purpose: Separate the construction of a complex object from its representation so that the same construction process can create different representations.

Create/defines VehicleBuilder : all the method to build car or van
Call the method in sequence to create the final complex object is the job of director, this director will return the final complex object