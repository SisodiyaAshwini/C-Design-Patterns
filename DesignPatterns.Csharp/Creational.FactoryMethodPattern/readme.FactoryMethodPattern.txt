Purpose: Define an interface for creating an object, but let subclasses decide which class to instantiate.

A separate class whose responsibility it is to manage the instantiation. This separate class is known as a Factory.
